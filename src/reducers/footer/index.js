const initState = {
    data: [],
};
const FooterReducer = (state = initState, action) => {
    let newState = {};
    Object.assign(newState, state);
    switch (action.type){

    }
    return newState;
};

export default FooterReducer